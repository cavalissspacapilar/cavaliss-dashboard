import { NextResponse } from "next/server";

// TODO: ConversacionWhatsApp se conectará cuando Base44 empiece a registrar conversaciones
export async function GET() {
  return NextResponse.json([]);
}
